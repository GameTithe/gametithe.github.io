---
title: "[알고리즘] 백준 16171번: 나는 친구가 적다 (C++)"
date: 2025-07-16
toc: true
categories:
  - "Tistory"
tags:
  - "tistory"
---

브론즈 문제임에도 가져온 이유는

문제를 풀면서 알게된 점이 있어서 입니다.

처음에는 이렇게 풀었습니다.

나쁘지 않은 코드죠.

vector에 숫자를 뺀, 문자들을 따로 저장하고, 이것들로 문자열을 다시 만들고,

string.find를 하는 것입니다.

```
#include <iostream>
#include <cstring>
#include <string>
#include <vector>
#include <map>
#include <queue>
#include <algorithm>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	string str;
	string str2;
	vector<char> vec;
	cin >> str;
	cin >> str2;
	int idx = 0;
	for (int i = 0; i < str.length(); i++)
	{
		if (str[i] >= '0' && str[i] <= '9') continue;
		vec.push_back(str[i]);
	}

	str = string(vec.begin(), vec.end());

	if (str.find(str2) != string::npos)
	{
		cout << "1";
	}
	else cout << "0";
}
```

정석 풀이는 알고리듬은 동일한데

이렇게 vector 대신 string을 만들어서 += 연산자를 사용하더군요

이게 되는지 몰랐습니다 하하하

```
for (int i = 0; i < str.length(); i++)
{
    if (str[i] >= '0' && str[i] <= '9') continue;

    str3 += str[i];
}
```